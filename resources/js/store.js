import { createStore } from 'vuex';

const store = createStore({
    state() {
        return {
            // Definisci il tuo stato qui
        };
    },
    mutations: {
        // Definisci le tue mutazioni qui
    },
    actions: {
        // Definisci le tue azioni qui
    }
});

export default store;
